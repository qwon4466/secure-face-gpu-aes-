# SecureFace-RX + SCRFD-ARC 통합 시스템

# 얼굴 탐지 + 인식 (SCRFD / ArcFace)
insightface>=0.7.3
onnxruntime>=1.16.0       # CPU 추론 (GPU: onnxruntime-gpu)

# Hailo-8L 가속 (라즈베리파이 5, USE_HAILO=1일 때만 필요)
# hailo_platform은 PyPI에 없음 — Hailo 공식 설치 가이드로 라즈베리파이에
# HailoRT + 파이썬 바인딩을 설치해야 함 (pip install이 아니라 apt/dpkg 패키지).
# https://hailo.ai/developer-zone/ 참고. PC 개발 환경에서는 설치 불필요.

# INN 익명화 (PRO-Face S)
torch>=2.0.0
torchvision>=0.15.0

# 이미지 처리
opencv-python>=4.8.0
Pillow>=10.0.0
numpy>=1.24.0
scipy>=1.10.0

# 웹 서버 (등록 UI)
fastapi>=0.100.0
uvicorn>=0.23.0
jinja2>=3.1.0
python-multipart>=0.0.6
pydantic>=2.0.0

# 원격 GPU 복원 서버(PC)로 청크 전송 (main.py의 _remote_gpu_restore)
requests>=2.28.0
